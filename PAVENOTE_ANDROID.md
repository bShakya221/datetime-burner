# PaveNote Android Prototype

This feature branch contains a bundled Android camera prototype and a GitHub Actions workflow that builds its debug APK. The application intentionally omits the Android INTERNET permission and uses only the explicit on-device speech-recognition API.
